{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Server",
            "type": "shell",
            "command": "clang++",
            "args": ["-std=c++17", "src/*.cpp", "-pthread", "-o", "build/server"],
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}
